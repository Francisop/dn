# Core dependencies for drone RTSP/WebRTC detection project
# Note: PyTorch should be installed separately with CUDA support:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# Computer Vision & ML
opencv-python>=4.8.0
ultralytics>=8.0.0
numpy>=1.24.0

# WebRTC & Streaming
aiortc>=1.5.0
av>=10.0.0

# Web Server & CORS
aiohttp>=3.9.0
aiohttp-cors>=0.7.0

# If you already installed torch with CUDA, you can skip it in this file
# torch>=2.0.0
# torchvision>=0.15.0